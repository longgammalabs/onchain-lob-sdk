import BigNumber from 'bignumber.js';
export declare const binarySearch: (f: (x: BigNumber) => BigNumber, target: BigNumber, left: BigNumber, right: BigNumber, precision?: number) => BigNumber | null;
